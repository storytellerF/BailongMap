events {}

http {
    access_log /dev/stdout;
    error_log /dev/stderr warn;

    server {
        listen 80;
        root /usr/share/nginx/html;

        location /tiles/ {
            try_files $uri /tile.png;
        }

        location = /tile.png {
            root /etc/nginx;
        }
    }
}
